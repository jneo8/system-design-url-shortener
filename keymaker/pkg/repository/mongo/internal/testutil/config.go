package testutil

const (
	// MongoConnectionStr only for testing
	MongoConnectionStr = "mongodb://localhost:27017"
	// MongoDBName only for testing
	MongoDBName = "test_keymaker"
	// KeyCollection only for testing
	KeyCollection = "shortURL"
)
